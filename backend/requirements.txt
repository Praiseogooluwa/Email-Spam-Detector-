# Build essentials (must be first)
setuptools==69.5.1
wheel==0.43.0
pip==24.0

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.0.0

# File handling
python-multipart==0.0.6

# ML dependencies (free tier friendly versions)
scikit-learn==1.3.2
numpy==1.24.4

# Language detection
langdetect==1.0.9

# Email parsing (built-in, no extra install needed)
# email module is part of Python standard library
