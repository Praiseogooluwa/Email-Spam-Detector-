# Core dependencies (keep as-is)
fastapi==0.109.2
uvicorn==0.27.1

# Lightweight ML stack (updated for compatibility)
scikit-learn==1.3.2  # Downgraded for better stability
numpy==1.24.4        # Matches scikit-learn 1.3.2 requirements
joblib==1.3.1        # Required by scikit-learn

# Essential utilities
langdetect==1.0.9
python-multipart==0.0.6  # Required by FastAPI for file uploads

# Build system (minimal set)
setuptools==68.2.2  # Slightly older but more stable
wheel==0.42.0       # Compatible with setuptools
